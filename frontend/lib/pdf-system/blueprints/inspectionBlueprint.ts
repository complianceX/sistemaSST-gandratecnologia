import type { AutoTableFn, PdfContext } from "../core/types";

export async function drawInspectionBlueprint(_ctx: PdfContext, _autoTable: AutoTableFn) {
  void _ctx;
  void _autoTable;
  // Blueprint reservado para fase 3: Inspecao (operational)
}
