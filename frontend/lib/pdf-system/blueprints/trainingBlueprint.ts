import type { AutoTableFn, PdfContext } from "../core/types";

export async function drawTrainingBlueprint(_ctx: PdfContext, _autoTable: AutoTableFn) {
  // Blueprint reservado para fase 3: Treinamento (training)
}

