/**
 * Wrapper oficial do seed de carga.
 *
 * Mantém compatibilidade com o comando:
 *   node ./node_modules/ts-node/dist/bin.js -r tsconfig-paths/register test/load/seed-tenants.ts
 */
import '../../backend/test/load/seed-tenants';
