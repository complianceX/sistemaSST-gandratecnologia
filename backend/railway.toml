[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node scripts/run-migrations.js && node dist/main.js 2>&1"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
