# Infra local (docker-compose.local.yml)
# Gere um arquivo `.env.local` (não comite) com valores fortes.

LOCAL_POSTGRES_USER=postgres
LOCAL_POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
LOCAL_POSTGRES_DB=minha-api

LOCAL_MINIO_ROOT_USER=minio
LOCAL_MINIO_ROOT_PASSWORD=CHANGE_ME_STRONG_PASSWORD

